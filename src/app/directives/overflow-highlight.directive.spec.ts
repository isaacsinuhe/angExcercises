import { OverflowHighlightDirective } from './overflow-highlight.directive';

describe('OverflowHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new OverflowHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
